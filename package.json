{
  "name": "game-platform-root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:sdk && npm run build:server && npm run build:admin && cp apps/server/package.json dist/",
    "build:sdk": "cd packages/sdk && npm run build",
    "build:admin": "cd apps/admin && npm run build",
    "build:server": "cd apps/server && npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "fast-glob": "^3.3.3",
    "typescript": "^5.0.0"
  }
}
